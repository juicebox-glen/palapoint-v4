'use client'

import { KinkFrameSkeletonDisplay } from '@/components/layout/KinkFrameSkeletonDisplay'

/** Full-bleed kink v2 skeleton for venue TV / Firestick — `/kink-frame/skeleton/display` */
export default function KinkFrameSkeletonDisplayPage() {
  return <KinkFrameSkeletonDisplay variant="device" />
}
